defmodule TokaiMonitorBackend.VideoStatisticCollectorTest do
  use ExUnit.Case
  doctest TokaiMonitorBackend.VideoStatisticCollector

  test "greets the world" do
    assert TokaiMonitorBackend.VideoStatisticCollector.hello() == :world
  end
end
